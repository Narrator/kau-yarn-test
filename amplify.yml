version: 1
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
    postBuild:
      commands:
        - non-existent-command
frontend:
  phases:
    preBuild:
      commands:
        - non-existent-command
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
